import React from 'react';
import PropTypes from 'prop-types';

const ComposeFlashcard = props => {
  return (
    <div>
      COMPOSE!
    </div>
  );
};

ComposeFlashcard.propTypes = {
  
};

export default ComposeFlashcard;